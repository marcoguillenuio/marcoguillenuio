name: Job Deployment Success - Single Step
on:
  workflow_dispatch:
jobs:
  test-success:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: 'Deployment Success - Single Step'
        run: |
          echo "Success Deployment"